# Blankweave installer choices. Core environment and detected hardware packages
# are always installed. Profiles are whitespace-separated and order-independent.
version=1
profiles=desktop development communication
