#!/usr/bin/env bash
#
# Transitional shim: an installed hyprarch CLI fast-forwards its checkout and
# then execs bin/hyprarch from the new revision. That is this file, which
# hands over to the renamed command.
exec "$(dirname -- "$(readlink -f -- "${BASH_SOURCE[0]}")")/blankweave" "$@"
